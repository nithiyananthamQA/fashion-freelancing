# Source images

Original artwork as supplied, kept for future crops and re-exports.
Nothing here is served: the site uses the optimised derivatives under
`public-html/assets/img/`. This folder sits outside `public-html` on purpose,
because the build copies that directory whole.
